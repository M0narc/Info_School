from django.shortcuts import render

from .models import Docente
# Create your views here.

# Post docentes
# def docente_alta_view(request):
    # aca estamos por poner un "POST" que es una funcion de django para guardar la info

